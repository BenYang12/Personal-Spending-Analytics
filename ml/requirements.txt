# I pin minor versions so a rebuild months from now trains the same models.
# Reproducibility is the whole reason my seed data uses a fixed random seed too.
pandas==2.2.3
scikit-learn==1.5.2
numpy==2.1.3
requests==2.32.3
joblib==1.4.2
matplotlib==3.9.2
pytest==8.3.4
