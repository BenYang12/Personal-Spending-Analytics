# Where the trained artifacts live. I point at ../ml/models locally; in a real
# deployment this would be a mounted volume or an object-store path, which is
# exactly why it's configurable instead of hardcoded.
LEDGERLENS_MODEL_DIR=../ml/models
