# Server-side only. These are read in Server Components and the Plaid proxy
# route, never shipped to the browser — note the absence of the NEXT_PUBLIC_
# prefix, which is what would expose them.
LEDGERLENS_API_URL=http://localhost:8080
LEDGERLENS_API_KEY=your-api-key-here
